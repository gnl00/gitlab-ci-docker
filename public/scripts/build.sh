#!/bin/bash

echo "executing scripts/build.sh test"

# image_name=$1
# image_version=$2

# create jar
# mvn clean & package

# create docker image
# docker build -t "${image_name}":"${image_version}" .